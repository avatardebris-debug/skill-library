# /zero safety

- `/req` first: REQUIREMENTS rows; floor kinds not agent-deleted  
- Occam prune = requirement `propose_delete` + concept list + plan rows only  
- `propose_remove` / `propose_delete` never execute as live tree edits  
- Artifacts: `notes/zero/<slug>/` (includes REQUIREMENTS.md)  
- Clone/worktree only if human later approves implement  
- Original tree stays until explicit human cutover  
- Agent-binding “never” (auto-LFG, outer RSI, live delete) holds unless human waives a specific line this session  
